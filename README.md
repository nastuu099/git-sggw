# Pierwsze uruchomienie

Linux/macos
```bash
python3 -m venv venv
source ven/bin/activate
pip install -r requirements.txt
```
Windows
```bash
py -m venv venv
venv\Scripts\activate.bat
pip install -r requirements.txt
```
